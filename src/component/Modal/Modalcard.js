import React from 'react'

const Modalcard= () => {
    return (
      <div className="modal">
        <div className="modal-content">
          <p>You have added the maximum number of character. Remove some to add again.</p>
        </div>
      </div>
    );
  };

export default Modalcard
